<%= form_for @neighborhood do |f| %>
 
  <%= f.hidden_field :city_id, value: @city.id %> 
  <div>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.submit "Create New Neighborhood" %>
  </div>
<% end %>
